# テスト

```sh
go test ./...          # 単体テスト（-race 推奨）
go test -race ./...
go vet ./...
gofmt -l .             # 未整形ファイルの一覧（空なら整形済み）
```

テストはネットワーク・SSH を使わず、ローカルの `git` で作成したリポジトリのみで完結
します（実行には `git` が必要です）。
